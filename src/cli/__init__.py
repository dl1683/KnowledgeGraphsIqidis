"""
Command-line interface tools for the Knowledge Graph system.

These tools can be run directly from the command line:
    python -m src.cli.extract --matter my_case --dir ./documents
    python -m src.cli.query --matter my_case "Who are the parties?"
    python -m src.cli.export --matter my_case --output graph.json
"""
